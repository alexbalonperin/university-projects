package model;

import java.awt.Color;
import java.util.ArrayList;

/*Cette interface implemente le pattern observer associe au compteur.
 L'implementation de ses fonctions permettra de mettre a jour les objets de la vue qui observent un compteur en leur transmettant les nouvelles valeurs des parametres.
 */

public interface ObserverCounter {
	public void update(int turn, ArrayList<Integer> points, ArrayList<Color> color);
}
